<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.zydse.mapper.QuestionMapperExt">
  <resultMap id="BaseResultMap" type="top.zydse.model.Question">
    <id column="ID" jdbcType="BIGINT" property="id" />
    <result column="TITLE" jdbcType="VARCHAR" property="title" />
    <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate" />
    <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified" />
    <result column="CREATOR" jdbcType="BIGINT" property="creator" />
    <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount" />
    <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount" />
    <result column="THUMB_COUNT" jdbcType="INTEGER" property="thumbCount" />
    <result column="TAGS" jdbcType="VARCHAR" property="tags" />
  </resultMap>
  <resultMap extends="BaseResultMap" id="ResultMapWithBLOBs" type="top.zydse.model.Question">
    <result column="DESCRIPTION" jdbcType="CLOB" property="description" />
  </resultMap>
  <update id="increaseViewCount" parameterType="top.zydse.model.Question">
    update QUESTION
    set
    VIEW_COUNT = VIEW_COUNT + #{viewCount},
    where
    id = #{id}
  </update>

  <update id="increaseThumbCount" parameterType="top.zydse.model.Question">
    update QUESTION
    set
    VIEW_COUNT = VIEW_COUNT + #{viewCount},
    where
    id = #{id}
  </update>

  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select *
    from QUESTION
    where ID = #{id,jdbcType=BIGINT}
  </select>

  <select id="countBySearch" parameterType="java.lang.String" resultType="java.lang.Integer">
    select count(*)
    from QUESTION
    <where>
      <if test="search != null and search != ''">
        and title regexp #{search}
      </if>
    </where>
  </select>

  <select id="selectBySearch" parameterType="top.zydse.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select *
    from QUESTION
    <where>
      <if test="search != null and search != ''">
        and title regexp #{search}
      </if>
    </where>
    order by gmt_create desc limit #{offset},#{size}
  </select>

</mapper>